name: Build APK

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: kivy/buildozer

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Build APK
      run: |
        buildozer -v android debug
